# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

GET     /cars                       controllers.CarsController.index
POST    /cars                       controllers.CarsController.create
GET     /cars/:id                   controllers.CarsController.show(id: java.util.UUID)
PUT     /cars/:id                   controllers.CarsController.update(id: java.util.UUID)
DELETE  /cars/:id                   controllers.CarsController.destroy(id: java.util.UUID)
